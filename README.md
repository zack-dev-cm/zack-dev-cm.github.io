Available at:
https://zack-dev-cm.github.io/

## Deployment
- GitHub Pages: `npm run build` (default base path is `/docs/`).
- Cloudflare Pages: see `cloudflare-deploy.md`.

## Tests
```bash
npm run build
npm run test:e2e
```
